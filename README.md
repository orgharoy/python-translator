This is a python program which takes in string inputs from a json file and then outputs the translation in a pdf file.

Uses google tralsate API



shotcomings: only works with languages with latin alphabets. 
